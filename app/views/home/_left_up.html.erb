<div class="row">
  <div class="col-md-3 col-xs-12">
    <aside>
    <h3>Cozinhas</h3>
      <% Cuisine.all.each do |cuisine| %>
        <p><%= link_to("#{cuisine.recipes.count} - #{cuisine.name}", cuisine)%>
      <% end %>

    <h3>Tipos</h3>
      <% RecipeType.all.each do |recipe_type| %>
        <p><%= link_to("#{recipe_type.recipes.count} - #{recipe_type.name}" , recipe_type) %>
      <% end %>
    </aside>
</div>
<div class="col-md-9" col-xs-12>
  <div class="row">
    <main>
